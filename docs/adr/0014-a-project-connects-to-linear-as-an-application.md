---
status: accepted
---

# A project connects to Linear as an application, and the key becomes the fallback

ADR-0012 settled where a project's Linear API key lives and why. It did not ask whether a
personal API key is the right credential, because at the time there was nothing to weigh:
Ogun read tickets and wrote nothing, so the only property that mattered was that the key
stayed on the machine that polls.

Writing back is next (§4.13's pipeline, §9's scope evaluator). The moment Ogun posts a
comment, a personal key stops being an implementation detail and becomes an
**attribution**: every comment appears **as the person whose key it is**, on a shared
board, with no way for their colleagues to tell the machine's words from theirs. That is
not a preference about tidiness. It is a person's name on text they did not write, in a
place other people make decisions from.

Linear's `actor` parameter fixes it — `actor=app` attributes writes to the application
rather than to whoever authorized it, and their documentation says the mode is for "agents
and service accounts". **A personal API key cannot do this at all.** There is no header, no
parameter, and no setting; the key *is* a person.

**So a project connects through an OAuth 2.0 application, and the personal API key remains
supported as a documented fallback.** The operator registers an application in their own
workspace, Ogun stores the client id and secret beside the key it already stores, and the
authorization produces a grant Ogun refreshes for itself. `readProjectSecret` grows states
rather than gaining a sibling: one function decides which credential a poll uses, so
nothing else in the system can hold a second opinion about it.

Scope is the second reason, and it is worth stating separately because it does not depend
on writing anything. A personal key is *everything that person can do* in that workspace,
forever. This asks for `read` and nothing else, and it will ask for `comments:create` when
the code that posts a comment lands — which is Linear's own advice ("If your application
only needs to create comments, use a more targeted scope"). The cost of that is
re-authorization when the scope widens, and it is the right cost: a scope is approved by
somebody reading a consent screen, and asking for a permission the current build cannot use
spends their attention on nothing.

Ogun ships **no client id**. It is self-hosted, so there is no Ogun-the-company to register
an application on anybody's behalf, and there is nowhere to keep a client secret that every
installation would share. Each workspace registers its own at
`https://linear.app/settings/api/applications/new`. That has one consequence which drives
most of the UI: the operator has to paste a **redirect callback URL** into Linear's form,
Linear matches it exactly, and its error for a mismatch names nothing useful — so the
string is generated by the server that will receive the callback and shown with a copy
button, rather than documented and left to be retyped.

## The five decisions this record exists for

### 1. When the token is refreshed

Access tokens last 24 hours. A source polls every five minutes while the control plane is
up, on a machine that is a workstation that gets closed, a VPS that gets restarted, or a
laptop that sleeps. So the interesting case is not "the token expires while we are
watching" — it is **"the process starts, or wakes, holding a token that died some hours
ago"**, which is the ordinary case rather than the edge one.

**Refresh on demand, immediately before the poll that needs a token.** The check is
`credentialHealth` against a ten-minute horizon, which is the same function and the same
shape the credential preflight uses — so a token with four minutes left is renewed rather
than used and lost between page two and page three of a paged read.

- **A timer rejected.** It refreshes in a process that may not be running: the machine
  asleep since yesterday evening has no timer, and the token is dead before the first tick
  would have fired. It also rotates when nothing needs a token, spending a chance to lose
  one on a night no source was due.
- **Reacting to a 401 rejected.** The poll has already spent a request, and Linear answers
  an expired token with the same `AUTHENTICATION_ERROR` it answers a revoked one — so the
  reactive path cannot tell "renew this" from "this connection is over" without trying.
  ADR-0010 already declined this shape for the model providers, where the answer was to
  catch the lapse before it costs a night.

**How this sits with the credential preflight.** It does not, and that is deliberate.
`fleetCredentials` and admission refuse a *job* whose model-provider credential will not
outlive it. A Linear grant is not part of that and must not become part of it: refusing a
job over a Linear token would be refusing work for a credential most jobs do not use.
**[amended]** The original reason given — that nothing about Linear reaches a sandbox —
stopped being true when a worker gained `connections: [linear]` (ADR-0013, Consequences),
and the decision survives the reason changing. A job that *was* granted a connection and
finds no live grant is answered by the gateway with a `502` naming the cause, at the moment
it asks, rather than being refused before it starts; the alternative is a preflight that
refuses a whole job because one optional call would have failed. Whether a granted job
should be preflighted like a model credential is an open edge and would be a real finding.
What is shared is the *vocabulary* — `CredentialExpiry`, `credentialHealth`,
`humanDuration`, and now `GRANT_REFRESH_HORIZON_MS` and `describeGrant`, all in
`credentials.ts` — so "expires in 42 minutes" means the same thing in `doctor` whether it
is about Anthropic or about a workspace. **The grant gates the poll; the preflight gates
the job.**

**The ADR-0010 trap that does not apply.** ADR-0010 rejected refreshing an OAuth token
because the provider **rotates the refresh token on use** — and Linear does, explicitly.
There, the credential was *borrowed* from `~/.claude/.credentials.json`, a file a human's
own `claude` also reads and writes, so spending the refresh token logged them out of their
own CLI by a background process they did not know existed, with a symptom ("my Claude login
keeps dropping") nowhere near the cause. **None of that holds here.** Ogun obtained this
grant for itself, through a flow it ran; no other program on the machine has a copy; there
is no user-facing CLI whose session this can end. Rotation costs nothing because nobody
else is holding the thing being rotated.

**The trap that does apply** is the second one ADR-0010 named: two writers racing over a
credential file. It is handled rather than argued away — the write goes through
`storeOAuthGrant` and therefore `updateLocalConfig`'s lock, the same lock `ogun project
add` and `ogun runner join` take, and which was itself recently fixed for a race where it
deleted a live lock. Three further rules fall out of rotation and are each a test:

- A **transport** failure never deletes the stored refresh token. Linear's 30-minute replay
  window for a consumed refresh token exists precisely for a response that did not arrive,
  and only a client that still has the token can use it. A client that cleared its token on
  a timeout has destroyed a connection over a blip — and reconnecting needs a workspace
  admin, so the recovery is not one click.
- Nothing deletes a grant on a *permanent* failure either. An operator disconnects
  deliberately, from a surface that says what it is doing; a poller erasing a credential at
  3am is the destructive write ADR-0012 keeps `empty` separate from `absent` to make
  visible.
- The new pair is **written before it is used**, and a failed write stops the poll. The
  rotation has already happened at Linear, so proceeding would mean the next poll replays a
  spent token; stopping keeps the failure inside the replay window, where a retry recovers
  the same pair.

Refreshes are single-flighted per project across the process, because `main.ts` drives
`pollSources` from an interval that does not await its callback and two sources in one
project can therefore meet the same expiring grant. The residual race — two *processes*
sharing one config.json — is not a supported configuration, and the replay window means
even that recovers.

### 2. The `state` parameter

Without one, `GET /api/oauth/linear/callback?code=<attacker's code>` is a URL anyone can
make the operator's browser visit: an image tag on any page, a link in a ticket. The
control plane would exchange that code, store the grant, and Ogun would be polling **the
attacker's** workspace while the operator believes it is polling theirs. Nothing downstream
can detect it — the code is valid, the exchange succeeds, the tokens work.

So: 32 bytes from `randomBytes`, the same source and width as the admin token; stored
server-side in a map; **single-use**, deleted on read; and **expiring** after ten minutes,
with abandoned entries swept on every use so the map cannot grow from closed tabs.

Stored server-side rather than encoded into the parameter, because the entry has to carry
facts the callback must not be able to choose: **which project** the authorization was
started for, and **which redirect URI** was sent. A `state=<slug>:<nonce>` encoding needs no
map and reopens the hole from a different angle — a valid nonce presented with a different
slug would file a grant under the wrong project.

In memory rather than in `~/.ogun/config.json`: the flow begins and ends in one process
within seconds, and persisting a short-lived nonce would put it in a 0600 credential file
and add a second writer to it. A restart mid-flow makes the callback fail with "start
again", which is correct and rare.

**A callback whose state does not match is refused before the token exchange, with nothing
stored and the code never spent.** The ordering is the whole value: an unrecognised state
means the code is not ours, and exchanging first and asking afterwards would already have
created the grant the check exists to prevent. The browser is redirected to
`/settings?linear=state`, which names no project — without a valid state there is nothing
that says which project the callback was ever about.

The callback is also **exempt from the admin token**, which looks like a hole and is the
opposite. The session cookie is `SameSite=Strict` and a redirect from `linear.app` is a
cross-site navigation, so the browser does not send it; requiring the admin token would
make the flow impossible on every control plane that has one, which is every control plane
bound beyond localhost, which is the deployment this feature exists for. `scopeForPath`
classifies it the way it classifies `/runners/join` — a route that authenticates its own
request — and the credential it checks is the state.

### 3. The authorization code in the callback URL

**`hono/logger` logs the query string.** That was read out of the middleware's source
rather than inherited from ADR-0012's paraphrase of it: it computes
`url.slice(url.indexOf('/', 8))` — everything after the host, query included — and writes
it twice, incoming and outgoing. ADR-0012's amendment says "method, path and status", which
is close enough for a route whose secret is in the body and wrong for one whose secret is
in the query. That line has been corrected in place, in `auth.ts`, marked as a correction
rather than quietly fixed.

All three available responses are taken, because they cover different halves:

1. **The route is exempt from the logger.** Silence was rejected — a callback that arrived
   and failed has to leave evidence, and a missing line is indistinguishable from a
   callback Linear never sent — so a substitute line records the method, the path, the
   status, and that the query was withheld and why. A person grepping the journal for the
   code finds the sentence explaining its absence.
2. **It redirects immediately to a clean URL**, so the code leaves the address bar, the
   history, and any onward `Referer`.
3. **No response body and no error message contains the code.** The failure redirect
   carries a short reason *code* from a closed set; the detail is kept server-side and
   fetched by an authenticated request. A message in a URL is a message in a screenshot,
   and part of this one would have been written by an upstream server out of the parameters
   we sent it.

What is deliberately **not** done is gating the callback on the transport the way
`secretWriteTransport` gates the secrets route. That gate exists for a long-lived
credential in a third party's workspace crossing a LAN in cleartext. An authorization code
is single-use, expires in minutes, and cannot be redeemed without the client secret — which
travels only from this machine to Linear, never across the operator's network. **The client
secret is gated**, by the same function, on the route that accepts it, and consistency with
ADR-0012 is that the rule follows the value rather than the endpoint.

### 4. Storage shape

`client_id` is not a secret — it is in every authorization URL the browser visits and on
Linear's own settings page — and hiding it would leave `doctor` and Settings unable to say
*which application* a project is connected as. `client_secret`, the access token and the
refresh token are secrets, and only the first is ever typed by a person.

**`SECRET_NAMES` stays `['linear']`.** Adding `linear-oauth-client-secret` was the first
shape tried and it fails through the exact door the closed set was built to close. Those
names are what the Settings form offers and what `list` reports as `set` — and a client
secret stored on its own authenticates nothing without a client id beside it and a grant
obtained with the pair. An operator would paste one, see the row go green, see the CLI
agree, and have a project that authenticates to nothing: "a secret nothing reads looks
exactly like one that works, right up until the night it mattered", arriving through the
listing that sentence is written under. The closed set therefore stays about *values used
verbatim as credentials*.

**The grant lives in a second block of the same file**, `oauth`, keyed by project slug then
provider — beside `secrets`, written through the same `updateLocalConfig` lock, read and
written by the same module (`config/secrets.ts`), and declared in `localConfigSchema` for
the same reason `secrets` is: zod strips what a schema does not name, so an undeclared
block would be silently deleted by the next `ogun project add`, and the symptom would be a
Linear connection that stopped working on the day somebody registered an unrelated
repository.

**`readProjectSecret`'s union grows instead of a second function appearing.** It returns
`granted | present | unconnected | absent | empty | malformed | unreadable` — seven states,
because each has a different remedy, which is principle 6's actual rule rather than a
target number. The three that are new:

- `granted` carries the grant and `apiKeyIgnored`, so every surface can say whether a
  personal key is sitting behind it doing nothing.
- `unconnected` is an application registered with the authorization never finished, and no
  key to fall back to. The fix is a browser rather than a terminal, and reporting it as
  `absent` would send somebody who has done most of the work off to paste a personal key
  instead.
- `malformed` is *this project's* entry being unreadable, kept apart from `unreadable`,
  which is the whole file. One means reconnect one project; the other means a config.json
  currently failing to parse for everything on the machine, where "set it again" sends
  someone to overwrite a file that is already broken.

Adding the variants made `sources.ts` fail to compile, which is the point of a union: the
compiler asked for a sentence for each new state rather than letting them fall into an
existing one.

### 5. Which credential wins

**An OAuth grant wins over a personal API key.** A grant exists only because somebody
deliberately ran an authorization flow and a workspace admin approved an application; a
personal key is very often a leftover, still in the store because nothing asked for it to
be removed. Preferring the leftover would mean the connection an operator just made
silently does nothing — and, once write-back lands, that every comment still appears under
their name after they connected an application precisely so that it would not.

Precedence is decided **inside `readProjectSecret`**, once, so the poll, `doctor`, the CLI
and the UI cannot disagree about it.

Falling *back* to a key when a grant has expired is rejected outright. A grant whose
refresh has stopped working is a fact the operator has to see; authenticating as a person
instead hides a broken connection behind a working poll and changes attribution without
anyone asking. A dead grant refuses the poll and names the credential that died.

An application that is registered and *never connected* does **not** shadow a working key.
That is the ordinary state of a project halfway through the migration, and refusing it
would break a working poll to make a point.

**And the operator can tell**, in four places, because silence about which credential is in
use is how someone spends an hour debugging the wrong one:

- `ogun runner doctor` prints the workspace, the actor, the scopes and the remaining life,
  and says in as many words when a stored API key is not being used.
- `ogun linear status` says the same from the store, without needing the server —
  which is when the question is usually asked.
- The Settings card shows the same row.
- A `401` from Linear now names the credential — "linear rejected the oauth access token
  for Acme" versus "the personal api key" — because the client is built from a
  discriminated union that knows which one it holds.

## Considered Options

- **Keep the personal API key and do nothing.** The status quo, and it is fine for reading.
  It fails the moment anything is written: attribution is a property of the credential and
  there is no way to override it. Deferring until write-back lands was tempting and loses
  on ordering — the credential has to be in place, refreshing, and visible *before* the
  first comment is posted, or the first thing the pipeline does in a real workspace is post
  under somebody's name.

- **Ship an Ogun client id, so nobody has to register an application.** How every hosted
  integration works, and it cannot work here. There is no hosted Ogun; a client secret
  compiled into a self-hosted binary is a client secret published, and OAuth's entire
  premise is that the token endpoint can tell the application apart from everyone else.
  Distributing one would mean any Ogun installation could impersonate any other's
  application. The cost is the registration step, and the redirect URI copy button is what
  makes it survivable.

- **PKCE instead of a client secret.** PKCE exists so a client that *cannot* keep a secret —
  a single-page app, a mobile binary — can still do this flow. Ogun is a server holding the
  secret in a 0600 file on the same machine as the token exchange, so PKCE would add a code
  verifier to protect a channel whose other end is the same process. Rejected as machinery
  with nothing to defend. It is worth noting that Linear supports it, and that a future
  design where the browser drives the exchange would need it.

- **The `client_credentials` grant, which needs no browser at all.** Genuinely attractive
  for a headless control plane: one request, an app-actor token, no consent screen. Rejected
  because the token it returns has **no refresh token** and lasts 30 days, and Linear's
  guidance is to react to a 401 — which is the reactive design rejected in decision 1,
  with a 30-day fuse instead of a 24-hour one. It also grants access to all public teams
  rather than what a person approved. A token with no way to renew it is refused explicitly
  rather than falling through, in both the client and the store, because it is the shape a
  plausible future edit would produce.

- **`actor=user`, or making the actor configurable.** The default, and it needs no
  workspace admin — which is a real advantage, because `actor=app` is a workspace-level
  install and Linear requires an admin to approve it. Rejected as the default because it
  reintroduces exactly the problem this ADR exists for, and rejected as a *setting* because
  `actor` is fixed at authorization time and baked into the tokens: a flag would mean two
  kinds of grant in the store, two attributions in Linear, and an effect invisible until
  months later when something writes. The operator who is not an admin is not stranded —
  they keep the personal API key, which is why it is kept rather than removed, and both the
  CLI and the UI say so at the point the install would fail.

- **Requesting `write` or `comments:create` now, to avoid re-authorizing later.** Rejected,
  and it is the option a reviewer is most likely to think is an oversight. Re-authorizing is
  a real cost paid by a real person. It is smaller than the alternative: a consent screen
  asking a workspace admin to let an application create comments, at a moment when no code
  can create one, spends their trust on a claim that is not yet true — and makes the next
  request, the one that is real, indistinguishable from this one.

- **Storing the grant in postgres.** Rejected by ADR-0012, whose argument is untouched and
  is stronger here: a grant is three secrets rather than one, and `projects` is a row that
  already travels through `GET /api/projects`.

- **Storing the grant as a JSON string inside the existing `secrets` map**, which needs no
  schema change at all. Rejected on three counts. `listProjectSecrets` would report it as a
  key, the Settings form would offer to overwrite it with a paste, and — the one that
  decides it — the expiry would be inside an opaque sealed value, so `doctor` would have to
  `expose()` a token to read the number beside it, and ADR-0012's "expose() appears once per
  consumer, at the wire" would be false by construction.

- **Refreshing in the gateway, the way ADR-0010 discussed.** Rejected, and still rejected —
  the gateway does not refresh anything, here or for the model providers. **[amended]** The
  reason originally given was wider than the decision: it said a gateway that knew about
  `api.linear.app` at all would be a symptom rather than a feature. A later slice made the
  gateway splice this grant into *sandbox* traffic on purpose, so that a skill can act on a
  ticket it was already given (ADR-0013, Consequences). What that changes is who may send a
  request; what it does not change is who renews the token. The renewal stays where this
  record puts it — on the control plane, on demand, immediately before the poll that needs
  one — and the gateway re-reads the store through `readProjectSecret` exactly as it
  re-reads `~/.claude/.credentials.json` for Anthropic. One writer, one refresh path.

## Consequences

- **The Linear client has one seam and two credential shapes.** `LinearCredential` is a
  discriminated union, because the two disagree about one header — a personal key is sent
  **raw**, an OAuth token **with `Bearer`** — and both mistakes are silent: each produces a
  well-formed request and the same `AUTHENTICATION_ERROR` Linear answers a revoked
  credential with. A boolean `bearer: true` beside a `token: string` would compile and would
  eventually be defaulted wrong. The union also carries what to *call* the credential in a
  failure, which is what makes a 401 name the right thing to rotate.

- **`~/.ogun/config.json` gains an `oauth` block, and no migration exists.** Nothing about
  this touches the database. The last migration is still `0016_run_rounds`.

- **A new environment variable, `OGUN_PUBLIC_URL`.** The redirect URI is derived from the
  address the browser used, because `OGUN_BIND` is `0.0.0.0` on exactly the deployments
  where it matters and `0.0.0.0` is not a URL anybody can open. Behind a TLS-terminating
  proxy the request we see carries the wrong scheme, and Linear matches the string exactly —
  so the operator declares the public address. `POST /start` refuses when the address in use
  is not the one the application was registered with, rather than authorizing into a failure
  Linear will not explain.

- **`describeGrant` is deliberately reassuring where the gateway's equivalent is alarming.**
  An expired Linear access token is an `ok` line saying the next poll renews it. An expired
  Anthropic token is a warning, because **nothing renews that one** — the gateway re-reads a
  file the host's own CLI refreshes. Warning about a self-healing state teaches an operator
  to ignore the line on the night it means something. What is not claimed is that the
  renewal will succeed: a refresh token can be revoked, and the thing that finds out is the
  poll, which records a refusal naming the connection.

- **Nothing has run against a live Linear application, and this is the second honest gap of
  its kind.** There is no application registered to this project and no client secret on any
  machine here, so the flow has never been executed end to end. `test/linear-oauth-fixtures.ts`
  records, in full, which facts were taken from Linear's documentation on 2026-08-25 and what
  the fixtures prove: the request shapes, the units, the delimiters, the failure
  classification and the redaction. It does not prove Linear's live server behaves as
  documented. **The first real application registered against this project should be run
  through the flow once, by hand, and any difference recorded by fixing the fixture rather
  than the parser.** The two likeliest surprises are named there: the token endpoint's error
  body, which Linear documents only for a grant Ogun does not use, and whether a `read`-only
  `actor=app` install can read issues at all — which the documentation implies and nothing
  here has observed.

- **`hono/logger`'s behaviour is now a test rather than a comment.** A unit test asserts that
  it writes the query string, so that if a future version stops, the exemption in `app.ts`
  becomes visibly unnecessary rather than invisibly load-bearing.

- **The personal API key path is untouched and stays supported.** Every rule in ADR-0012
  still holds for it, `ogun secret set linear` still writes the file with no server running,
  and the transport gate is unchanged. Removing it would strand any operator who is not an
  admin of their workspace, which is not a rare configuration.

- **The command is `ogun linear`, and its usage lines say what it asks for.** It shipped as
  `ogun project linear app <project>`, which is four words in front of a verb: `project` was
  a namespace whose only cargo was the `<project>` positional, `linear` names the
  integration, `app` names the thing being registered, and then the slug again. Inferring
  the project from the directory — as `ogun project add` and `ogun project sync` always
  have, and as `ogun secret` now does — empties the outer level, so the outer level is gone.
  `linear` stays, and is the one noun in that chain that earns its place: it says *which*
  integration, which becomes a real distinction the first time a `github` source sits beside
  it. The old spelling is dropped rather than aliased and answers with a line naming the new
  one.

  The second half matters more. `ogun project linear app <project>` read as a complete
  command and was not: the two things it exists to collect — a Client ID and a Client Secret
  — appeared nowhere in it, so the only way to find out that it prompts was to run it. That
  is the same fault `ogun secret set <name>` had and it lands harder here, because there are
  two values, one of them is a credential, and the application has to have been created in
  Linear's web UI before the first prompt is any use. The usage lines and the per-subcommand
  help now name the inputs, and the Settings page's refusal says so too, since a command an
  operator is sent to a terminal to run should not surprise them when they get there.

  `app` and `connect` refuse a slug the control plane does not know — before the first
  prompt — and list the ones that would have worked. `ogun secret set` acquired the same
  check against weaker evidence, with an `--allow-unregistered` escape, because it reaches
  no server by design; these two cannot run without the control plane at all, so the
  database is available and no escape is needed. The rule, stated once so the asymmetry does
  not read as an accident: **each command checks the slug against the best oracle it already
  depends on.** `disconnect` checks nothing, exactly as `ogun secret rm` checks nothing — a
  row `status` shows has to be a row you can remove, or a stranded refresh token stays in
  the file with the listing still advertising it.

  Nothing about the shape assumes a browser. `connect` prints consent instructions because
  `/api/oauth/linear/start` answered with an `authorizeUrl`, not because the command is
  built around one, and a start that comes back already connected is reported as such. If
  the `client_credentials` grant rejected above is ever adopted as the default, it is a
  server-side change and an operator types the same words.

- **Write scopes are the open edge, named on purpose.** This ADR settles that Ogun connects
  as an application and asks for `read`. It does **not** settle what write-back looks like:
  which scope, when a comment is posted, or how a comment is made idempotent given that
  `source_emissions` is currently the only thing standing between one card and 288 runs a
  day. A finding that Ogun should request `comments:create` is not a finding until the code
  that posts a comment exists.
