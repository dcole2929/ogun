# Static assets

`favicon.svg` is the source. It is drawn for 16px first — one silhouette, no interior
detail, and a background plate so it holds against a light or a dark browser tab.

There is deliberately no pile of PNG sizes. Every browser Ogun targets supports SVG
favicons, and a raster set is a set of files that drift from the source the first time
the mark changes.
